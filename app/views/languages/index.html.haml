.size-10
  %h1
    Languages
  = link_to '+', new_language_path, class: 'button create-button'

#filter
  = render 'filter'

%table.list.size-10
  %tbody.size-10
    %tr.size-10
      %th.size-2
        Name
      %th.size-4
        Type
      %th.size-3
        Designed by

    - @languages.each do |lang|
      %tr.size-10
        %td.size-2
          = link_to lang.name, language_path(lang)
          = link_to image_tag('trash.png'), language_path(lang), method: :delete, class: 'button delete-button'
        %td.size-4
          = lang.type
        %td.size-3
          = lang.designed_by